import React from "react";


// Récupération des tags
const Rate = ({rating}) => {
    console.log(rating);
    const score = rating.score;
    const notes=[1, 2, 3, 4, 5]
    return (
        <div>dzed</div>
    )
}

export default Rate

